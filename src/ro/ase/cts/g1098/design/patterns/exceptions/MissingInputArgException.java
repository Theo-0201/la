package ro.ase.cts.g1098.design.patterns.exceptions;

public class MissingInputArgException extends Exception {

	//if we extend exception, we need to handle it
	//if we extend runtime exception, we don't
	
	//we choose exception bcs we re in the building phase, and we want to let everyone know from the beginning
}
